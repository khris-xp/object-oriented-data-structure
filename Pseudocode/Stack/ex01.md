```
Exercise : 
ให้น้องๆเขียนโปรแกรมรับ input เป็นวงเล็บ โดยมีรูปแบบดังนี้  วงเล็บเปิด :  (  กับ  [    วงเล็บปิด :  )  กับ  ]   โดยให้หาว่าถ้าหากนำวงเล็บมาจับคู่กัน จะครบทุกคู่หรือไม่  โดยให้แสดงผลลัพธ์ที่บอกว่าคู่วงเล็บที่ Input เข้ามานั้น Match กันหรือไม่
```

### Pseudocode

```
def isParenthesis(string_input):
    ประกาศตัวแปร stack เป็น array

    ใช้ตัวแปร i วนลูปตามจำนวนตัวอักษรใน string_input:
        ถ้า i มีค่าเท่ากับ "(" หรือ มีค่าเท่ากับ "["
            เติมค่า i เข้าไปใน stack

        ถ้าแล้ว i เป็น ")" หรือ "]":
            ถ้า stack ว่าง:
                คืนค่า False
            แล้ว:
                ถ้า i เป็น ")" และ stack ตำแหน่งสุดท้าย เป็น "("
                    นำค่า stack ตำแหน่งสุดท้าย ออกจาก stack
                ถ้า i เป็น "]" และ stack ตำแหน่งสุดท้าย เป็น "[":
                    นำค่า stack ตำแหน่งสุดท้าย ออกจาก stack
                แล้ว:
                    คืนค่า False

    ถ้า stack ว่าง:
        คืนค่า True
    แล้ว:
        คืนค่า False
```